'use strict';

/**
 * prediction-challenge router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::prediction-challenge.prediction-challenge');
